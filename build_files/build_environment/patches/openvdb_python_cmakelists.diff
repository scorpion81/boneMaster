diff -Naur openvdb.orig/openvdb/python/CMakeLists.txt openvdb/openvdb/python/CMakeLists.txt
--- openvdb.orig/openvdb/python/CMakeLists.txt	2018-04-10 20:22:17.000000000 +0200
+++ openvdb/openvdb/python/CMakeLists.txt	2019-08-26 11:11:27.439427000 +0200
@@ -3,7 +3,14 @@
 
 FIND_PACKAGE ( PythonInterp REQUIRED )
 FIND_PACKAGE ( PythonLibs REQUIRED )
-FIND_PACKAGE ( Boost ${MINIMUM_BOOST_VERSION} REQUIRED COMPONENTS python )
+
+# MESSAGE ( "PYTHONLIBS_VERSION_STRING = ${PYTHONLIBS_VERSION_STRING}" )
+STRING(REPLACE "." ";" VERSION_LIST ${PYTHONLIBS_VERSION_STRING})
+LIST(GET VERSION_LIST 0 OPENVDB_PYTHON_VERSION_MAJOR)
+LIST(GET VERSION_LIST 1 OPENVDB_PYTHON_VERSION_MINOR)
+LIST(GET VERSION_LIST 2 OPENVDB_PYTHON_VERSION_PATCH)
+
+FIND_PACKAGE ( Boost ${MINIMUM_BOOST_VERSION} REQUIRED COMPONENTS python${OPENVDB_PYTHON_VERSION_MAJOR}${OPENVDB_PYTHON_VERSION_MINOR} )
 
 
 IF ( NOT OPENVDB_BUILD_CORE )
@@ -36,12 +43,6 @@
 INCLUDE_DIRECTORIES ( SYSTEM ${TBB_INCLUDE_DIR} )
 INCLUDE_DIRECTORIES ( SYSTEM ${ZLIB_INCLUDE_DIR} )
 INCLUDE_DIRECTORIES ( SYSTEM ${PYTHON_INCLUDE_DIR} )
-# MESSAGE ( "PYTHONLIBS_VERSION_STRING = ${PYTHONLIBS_VERSION_STRING}" )
-
-STRING(REPLACE "." ";" VERSION_LIST ${PYTHONLIBS_VERSION_STRING})
-LIST(GET VERSION_LIST 0 OPENVDB_PYTHON_VERSION_MAJOR)
-LIST(GET VERSION_LIST 1 OPENVDB_PYTHON_VERSION_MINOR)
-LIST(GET VERSION_LIST 2 OPENVDB_PYTHON_VERSION_PATCH)
 
 # MESSAGE ( "OPENVDB_PYTHON_VERSION_MAJOR = ${OPENVDB_PYTHON_VERSION_MAJOR}" )
 # MESSAGE ( "OPENVDB_PYTHON_VERSION_MINOR = ${OPENVDB_PYTHON_VERSION_MINOR}" )
